
public class RandomTactics implements Tactics {

	public int readTactics() {
		double random = Math.random() * 3;

		int hand = 0;
		if(random < 1) {
			hand = Player.STONE;
		}else if(hand < 2) {
			hand = Player.SCISSOR;
		}else if (hand < 3) {
			hand = Player.PAPPER;
		}

		return hand;
	}
}
